<% if @recipe.liked_by?(current_user) %>
	<%= link_to "Unlike", recipe_like_path(@recipe,@like), method: :post, remote: true %>
<% else %>
	<%= link_to "Like", recipe_like_path(@recipe,@like), method: :post, remote: true %>
<% end %>
